import type { FastifyInstance } from 'fastify'

// Placeholder — integrations are handled by /api/v1/ads-integrations (platforms + CRMs)
export async function integrationsRoutes(_app: FastifyInstance) {}
